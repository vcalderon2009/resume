# name: GitHub Actions Demo
# run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
# on: [push]
# jobs:
#   Explore-GitHub-Actions:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
#       - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
#       - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
#       - name: Check out repository code
#         uses: actions/checkout@v3
#       - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
#       - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#       - name: List files in the repository
#         run: |
#           ls ${{ github.workspace }}
#       - run: echo "🍏 This job's status is ${{ job.status }}."

name: Docker Image CI
run-name: ${{ github.actor }} - Resume
on: [push]
jobs:
  build:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v3
      - name: Docker Compose Original Version
        run: docker compose version
      - name: Install Compose
        uses: ndeloof/install-docker-compose
        with:
          version: v2.3.3 # defaults to 'latest'
          legacy: true # will also install in PATH as `docker-compose`
      - name: Docker Compose Version
        run: docker compose version
      # - name: Build Docker image
      #   run: make build
      # - name: Create Resume
      #   run: |
      #     make run
      #     mkdir -p ./final_resumes
      #     cp ././resume/Victor_F_Calderon/Victor_F_Calderon_Resume.pdf ./final_resumes
      # - name: List files in the repository
      #   run: |
      #     ls ${{ github.workspace }}
      # - uses: actions/upload-artifact@v3
      #   with:
      #     name: Victor-Calderon-Resume
      #     path: ./final_resumes/Victor*.pdf
